# Review ledger — ansible-framework strict cycle (append-only)

One row per completed piece merged to `main`. The framework runs the same P0–P5 strict cycle as the
application repos (Director, 2026-07-27): Claude plans → Codex adversarially reviews (P2) → Codex
executes in an isolated worktree (P3) → Claude validates + live proofs (P4) → Director approves
(P4.5) → merge `--no-ff` `[audited <sha>]`. System Codex (`gpt-5.6-sol`) drives P2/P3.

| Piece | Summary | P2 | Audited | Proof | Merge | Notes |
|-------|---------|----|---------|-------|-------|-------|
| WDM-01 | Scaffold `applications/windows_disk_manager` — Windows sibling of `linux_disk_manager`, guard-only (v3.2.0 loader byte-identical, `platform` vmware\|aws + `disks:[]` contract, state-scoped validate guards, fail-closed present_windows) | REVISE×4 → **AGREE** r5 (15+ issues: silent-green gap, proxmox drop, exact drive-letter regex, byte-identity proof, full negative matrix) | 5fc1f62 | **Live VM `pdq-dev`:** positive `ok=14 changed=0`; negatives fail-closed / bad-platform / both-identities / duplicate all correct; grammar matrix verified. P4 caught + repaired 2 defects (`.gitignore` untrackable; canonicalization emitted literal `\1`) | 6cfda6b | First framework piece via the app-repo strict cycle |
| WDM-02a | present_windows provider stage 1 — resolve declared disks by unique_id, attach assert, online/writable fixup, fail-closed safety classification (canonical VOLUME bearers, UNKNOWN-first, allowlist {NTFS,FAT,FAT32,exFAT,ReFS}, exact OURS/PRISTINE, FOREIGN default), distinct-number assert; NON-destructive, still fail-closed (mutation → 02b) | REVISE×5 → **AGREE** r6 (12→3→2→2→2→0; silent-green closed, deterministic classifier per Director middle-path blank-safety) | 4111693 | **Live pdq-dev:** 3 real disks → PRISTINE, safety pipeline passed, deferred-fail (ok=22 changed=0); FOREIGN(OS disk)/unattached/AWS-deferred/empty-greens correct; **9 synthetic-fact classifier fixtures** all per spec; validate/defaults comment-only. P4 no defects | 197c5ca | Middle-path blank-safety (Director); online-before-classify |
| WDM-02b | MINIMAL trust-config disk provisioning — RIP OUT the 02a safety classifier (Director: config control is the operator's job); resolve/online/skip-if-ours(NTFS+label via partitions[].volumes[])/init→partition→format, force:false, NTFS-only. Role now actually provisions + is idempotent | REVISE×2 → **AGREE** r3 (9→1→0; corrected latent 02a top-level-.volumes traversal bug, NTFS-only, per-disk defaults) | 73048d8 | **Live pdq-dev:** formatted E:PDQDEPLOY/F:PDQINVENTORY/G:PDQSHARE NTFS (changed=3); idempotency re-run changed=0 (traversal fix proven); P4 no defects | 1abfec4 | Director rip-out-classifier decision; supersedes 02a classifier + planned 02c/02d |
