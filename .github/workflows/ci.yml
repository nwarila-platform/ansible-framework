---
# =============================================================================
# CI Workflow — Lint & Validate
# =============================================================================
# Runs on every pull request targeting main and on direct pushes to main.
# Jobs are intentionally independent so they run in parallel — a yamllint
# failure does not block ansible-lint from reporting its own findings.
#
# Required status checks (configure in branch protection):
#   - yamllint
#   - allowlist-check
#   - ansible-lint
#   - loader-identity-check
# =============================================================================

name: CI

'on':
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# Minimal permissions at workflow level — elevated per-job only where required
permissions:
  contents: read

# Limit concurrent runs per branch — cancel stale runs on new push
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:

  # ---------------------------------------------------------------------------
  # YAML Lint
  # ---------------------------------------------------------------------------
  yamllint:
    name: YAML Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Install yamllint
        run: pip install --no-cache-dir -r requirements-dev.txt

      - name: Run yamllint
        run: yamllint --config-file .yamllint.yml .

  # ---------------------------------------------------------------------------
  # Deny-All Allowlist
  # ---------------------------------------------------------------------------
  allowlist-check:
    name: Deny-All Allowlist
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Check deny-all allowlist
        run: make allowlist-check

  # ---------------------------------------------------------------------------
  # Shared Loader Byte Identity
  # ---------------------------------------------------------------------------
  loader-identity-check:
    name: Shared Loader Byte Identity
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Check application loader byte identity
        run: make loader-identity-check

      - name: Check loader role defaults naming convention
        run: make loader-defaults-convention-check

  # ---------------------------------------------------------------------------
  # Ansible Lint
  # ---------------------------------------------------------------------------
  ansible-lint:
    name: Ansible Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Install ansible-lint
        run: pip install --no-cache-dir -r requirements-dev.txt

      - name: Run ansible-lint
        run: make ansible-lint

  # ---------------------------------------------------------------------------
  # Pre-Commit (all pre-commit-stage hooks, blocking)
  # ---------------------------------------------------------------------------
  # Runs every pre-commit-stage hook to catch any checks developers may have
  # skipped locally, and fails the job when a hook fails. commit-msg-stage hooks
  # (conventional-pre-commit) only run on real commits, never under --all-files.
  # Only no-commit-to-branch is skipped: it guards interactive commits, and on
  # push events to main HEAD is main, which would fail the job by design.
  # ---------------------------------------------------------------------------
  pre-commit:
    name: Pre-Commit Hooks
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Install pre-commit
        run: pip install --no-cache-dir -r requirements-dev.txt

      - name: Run pre-commit
        run: pre-commit run --all-files
        env:
          # no-commit-to-branch protects interactive commits; CI never commits, and
          # on push events to main HEAD *is* main, which would fail this job by
          # design. Every other pre-commit-stage hook runs and gates for real.
          SKIP: no-commit-to-branch
