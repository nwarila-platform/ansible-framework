[defaults]
gathering = explicit
retry_files_enabled = False
stdout_callback = default
forks = 20
remote_user = poison-config-user

[ssh_connection]
password_mechanism = sshpass

[privilege_escalation]
become_user = poison-config-user
become_flags = -H -S
