---
# =========================================================================================== #
# File: '{{ role_path }}/vars/main.yml'
# --- [ Description ] ----------------------------------------------------------------------- #
# Internal role constants - NOT intended to be overridden by users.
#
# windows_disk_manager has no internal constants: its entire contract is the caller-facing
# `platform` + `disks` inputs (see defaults/main.yml). This file exists for role-skeleton
# parity and as the home for any future internal constants.
# =========================================================================================== #
