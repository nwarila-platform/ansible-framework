# ============================================================================================= #
# File: '{{ role_path }}/tasks/clean_windows.yml'
# --- [ Description ] ------------------------------------------------------------------------- #
# Windows / state=clean - no-op. This role owns no cache/staging artifacts. Disk state is NEVER
# torn down here — a 'clean' must never remove a data volume.
# ============================================================================================= #

- name: 'END | Report That Clean Has No Artefacts To Remove'
  ansible.builtin.debug:
    msg: 'No staging artifacts to clean; windows_disk_manager state=clean is a supported no-op.'
