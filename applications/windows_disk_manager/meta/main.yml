# =========================================================================================== #
# File: '{{ role_path }}/meta/main.yml'
# --- [ Description ] ----------------------------------------------------------------------- #
# Role metadata for windows_disk_manager. Description-only (style guide 8): NOT an enforcing
# argument spec. The role's tasks/validate.yml hook enforces its merged-config guards.
# =========================================================================================== #

galaxy_info:

  role_name: 'windows_disk_manager'
  namespace: 'ansible_framework'
  author: 'HellBomb'
  description: >-
    Guard-only Windows disk-manager scaffold with stable disk identity and drive-letter
    contract for VMware and AWS.

  license: 'MIT'
  min_ansible_version: '2.16'

  platforms:
    - name: 'Windows'

  galaxy_tags:
    - 'storage'
    - 'disk'
    - 'windows'
    - 'vmware'
    - 'aws'

allow_duplicates: false

dependencies: []
