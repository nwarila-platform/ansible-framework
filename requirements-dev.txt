# =============================================================================
# Development Dependencies
# =============================================================================
# Pin exact versions for reproducible local and CI environments.
# Update via: pip install --upgrade -r requirements-dev.txt
# Or let Dependabot open a PR automatically.
# =============================================================================

# YAML linting
yamllint==1.35.1

# Ansible linting and syntax checking
ansible-lint==26.4.0
ansible-core>=2.17,<2.19

# Pre-commit hook framework
pre-commit==4.0.1
